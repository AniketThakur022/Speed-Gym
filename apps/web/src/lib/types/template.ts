/**
 * SolveAlongTemplate — re-exported from the shared package.
 *
 * The recovered APK source defined these types locally. During the 2026-09-03
 * reseed the canonical copy moved to `@vmsg/shared-types`
 * (packages/shared-types/src/template.ts), which the backend Question Bank and
 * the RAG factory also compile against, so the content contract cannot drift
 * between producer and consumer. The verbatim original is preserved at
 * `recovered/exam-arena-src/src_lib_types_template.ts_cf55`.
 */
export * from "@vmsg/shared-types";
