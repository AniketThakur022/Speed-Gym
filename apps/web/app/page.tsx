export default function Home() {
  return (
    <main style={{ maxWidth: 640, margin: "0 auto", padding: "4rem 1.5rem" }}>
      <h1 style={{ color: "#C8FF5A", fontSize: "2.25rem", marginBottom: "0.5rem" }}>
        Exam Arena
      </h1>
      <p style={{ opacity: 0.8, lineHeight: 1.6 }}>
        Rebuild shell (Sprint 0). The offline practice loop, dashboard, and the
        recovered Exam Arena UI land here next — seeded from the 70 recovered
        sources in <code>recovered/exam-arena-src/</code>.
      </p>
    </main>
  );
}
