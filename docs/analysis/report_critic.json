{
 "contradictions": [
  "Content-blocker figures: VMSG_TECHNICAL_ARCHITECTURE.md (2026-07-04) lists '240 empty :Problem.question_text, ~15 PREREQUISITE_OF edges, 63.7% verifier false-positive rate' as live Sprint-0/1 launch blockers, but VMSG_CONTENT_READINESS.md (same date) and VMSG_PROGRESS_REPORT.pdf (2026-07-05) declare exactly those numbers stale artifacts of an old problems.jsonl export — actuals: 3 missing texts, 2,457 prerequisite links, 97.8% verified, 18 flagged keys already cleaned. The architecture doc's content-readiness gate section must be treated as superseded.",
  "Chatbot vs RAG: the architecture doc specifies the study chatbot as a runtime pgvector consumer (cosine >0.70, top-5); VMSG_RAG_Content_Factory.pdf records a scope lock dated 2026-07-17 that the chatbot does NOT consume RAG and the factory has zero runtime consumers. The later lock presumably wins, but no source explicitly says so.",
  "Graph label and count: architecture doc models :Technique nodes (473); the RAG factory PDF says the live DB uses :Skill (467) and the :Technique schema file is stale; pipeline-untitled.pdf still codes against :Technique throughout. 473 vs 467 is also unexplained.",
  "Edge semantics conflict: architecture canonicalizes PREREQUISITE_OF as technique→technique and REQUIRES as problem→technique; the RAG factory PDF describes Skill-PREREQUISITE_OF→Problem and REQUIRES/FRONTIER_OF/NEXT_TOPIC as Skill→Skill learning-order edges. Same relationship names, different domains/ranges — this must be resolved against the live graph before any Cypher is written.",
  "Neo4j hosting: architecture doc self-hosts Neo4j 5 Community on Droplet B with strict heap caps; pipeline-untitled.pdf assumes Neo4j AuraDB Free tier (its whole OOM analysis and benchmarking protocol depend on AuraDB's JVM limits and dashboard).",
  "Content QA mechanics: architecture describes a 5-gate pipeline with 2/2 'jester' LLM consensus (≥0.85) plus a hallucination gate (≥0.90) and a 5-value trust enum (QUARANTINED_SOFT/HARD); the RAG factory PDF describes a 7-stage auditor with 2-of-3 AI judges and a 4-stage ladder (QUARANTINED→SANDBOX→TRUSTED→LIVE). Gate count, judge scheme, and enum shape all differ.",
  "Extraction engine naming: architecture Station 3 = 'trive v2' grounded triplet extraction; RAG factory PDF Station 3 = LangExtract (piloted on one Tirthaji chapter). Unclear whether these are the same tool renamed or a replacement decision.",
  "Chunk-corpus scale: architecture references 9,686 chunks that Sprint 4 must consume; the progress report and RAG factory PDF both say 4,943 chunks seeded/existing. The 9,686 figure appears nowhere else.",
  "Template counts: three unreconciled numbers — architecture's Neo4j ProblemTemplate '~50', the content-readiness/RAG-PDF '~1,505 T1 templates', and architecture's '1,707 computable templates deployed'.",
  "Problem/explainer counts: architecture says 1,707 Problem/SolveAlong + 1,707 :Computable; content-readiness and RAG PDF say 807 Problems + 791 SolveAlongs (=1,598). Explainers: 590 in the content package, 269 in the preview-2 dashboard snapshot, 1,526 in MASTER_corpus.jsonl — three different layers with no documented mapping.",
  "Book scope: RAG factory PDF says 'six math books' (6 :Book nodes); preview-2.html covers 9 books; the actually-profiled MASTER_corpus.jsonl on disk holds 25 books / 64 source files / 19,619 questions. The preview dashboards embed a stale 2,491-record snapshot of a corpus that is now 4,805 records — the graph and package were built from a much smaller corpus than what now exists.",
  "Frontend vs backend API contract: the recovered Exam Arena APK freezes /api/v1 with Firebase-idToken auth (auth/google, auth/phone), dashboard/* reads and POST sync/{key}; the architecture doc specifies a different surface (/api/auth/register|login|scanner-login QR, 22 REST endpoints + 9 Socket.IO events, no Firebase anywhere). The APK also adds a 'banking' (IBPS/SBI/SSC/UPSC) exam domain absent from the brief's CAT/GMAT/GRE scope, and ships no websocket/duel client although multiplayer is a Phase-1 architecture deliverable.",
  "Vision-backlog size: the dashboards report ~139 unanswered_reason questions + 146 untranscribed figures (9-book slice); direct corpus profiling shows 4,133 needs_vision questions + 499 needs_chart_vision records + 544 record-level flags across 25 books. The real vision re-pass backlog is roughly 30x what the dashboards surface.",
  "People/roles: chat-barry says ME=Harsh Ahirrao (owner) receiving delivery from 'Barry Gaddar' whose GitHub/email is aniketthakur022 / thakuraniketsingh022@gmail.com; chat-leads says Aniket (that same email — which is also this session's user email) is the internal technical builder ('Claude = Aniket'). One source casts that identity as the sender, the other as the current user. Client identity also conflicts: 'Souvik' per the final quotation filename vs 'Tanish' as the client contact in the leads chat.",
  "Loss vs delivery: chat-leads (2026-09-01) says the laptop died with NO backup and everything must restart from scratch; chat-barry shows a comprehensive same-day delivery (Drive zip with scripts+dashboards, MASTER corpus, APK, all docs, Excalidraw). Substantial artifacts clearly survived on Drive/WhatsApp — what is actually lost is most plausibly the two git repos (E:\\claude\\Claude\\vedic-speed-gym and D:\\Exam-Arena-Frontend) and any un-exported local DB state, not 'everything'.",
  "os-taxonomy framing: the briefing/chat present withmarbleapp/os-taxonomy as tied to VMSG's extraction assets; web verification shows the public repo is Marble's unrelated elementary-curriculum taxonomy (1,590 topics, ODbL) containing no VMSG scripts or dashboard.",
  "Minor internal inconsistencies worth carrying: architecture §5.5 heading says 'three-path write' but lists 4 paths (diagram says 4-path); the progress report claims 9 commits but lists 8 hashes."
 ],
 "single_source_facts": [
  "Anthropic API key pasted in plaintext into WhatsApp on 2026-03-20 (chat-barry only) — rotate it regardless of verification.",
  "The 2026-07-17 RAG scope lock (batch-only nightly+hourly, zero runtime consumers, chatbot excluded) appears only in VMSG_RAG_Content_Factory.pdf — it silently overrides the architecture doc and the whole RAG workstream hangs on it.",
  "Ontology-registry embeddings are all zeros and must be regenerated (RAG factory PDF only).",
  "All psychometric constants (BKT priors 0.35/0.14/0.10/0.20, P(F)=0.007/day, Glicko-2/DINA/IRT parameters), tier prices $0/$6/$9.60/$12.60, and the 22-REST + 9-WS contract exist only in VMSG_TECHNICAL_ARCHITECTURE.md.",
  "Everything about the actual build state (Sprint 0 + Sprint 2 complete, 46/46 tests, 5/16 sutras, 3,585 Neo4j nodes + 4,943 chunks seeded live, 7 schema defects fixed, migrations 00–70, commit cfc2caf, repo at E:\\claude\\Claude\\vedic-speed-gym) comes only from VMSG_PROGRESS_REPORT.pdf — and that code is presumed lost with the laptop.",
  "MASTER_corpus.jsonl ground truth (4,805 records / 19,619 questions / 25 books / 68.5% keyed; Hall & Knight's 1,874 keys deliberately NOT extracted due to a corrupt scrambled answer grid) comes only from direct corpus profiling — no document reflects the 25-book corpus.",
  "gre_essays_corrected.jsonl is fully subsumed into MASTER_corpus.jsonl (corpus profiling only) — the do-not-re-ingest decision rests on this one check.",
  "The Exam Arena APK findings (mock-only mode with IS_MOCK default true, Firebase-idToken auth contract, ExamArenaDB Dexie schema with trusted=24h/sandbox=1h TTLs, SolveAlongTemplate zod schema with generationMethod template|llm|converted and 11 scaffold types, 70 recovered TypeScript sources now in the session scratchpad) come only from the APK teardown — the recovered sources are the ONLY surviving frontend code.",
  "The Google Drive folder (1ODMexaWbghZ7wbL8T6Y3-QKIMeedaT5p) as master delivery location — zip with extraction/enrichment scripts + tracking dashboard, pipeline PDF, explore.html and page_view.html ('not fully done') — is asserted only in chat-barry and is auth-gated; contents are entirely unverified.",
  "Client-facing history (two quote iterations, 2026-07-07 client meeting, 2026-07-20 'client closed' + advance received, obligation to tell Tanish about the loss, ₹22k Claude plan needed for the rebuild) exists only in chat-leads; the advance's attribution to VMSG is explicitly uncertain.",
  "Closure strategies A–D, the Neo4jDesignTimeCompiler script, taxonomy.yaml, and the sync_manifest/benchmark protocol exist only in pipeline-untitled.pdf; nothing confirms any of it was implemented.",
  "LLM providers (OpenRouter DeepSeek/Kimi for generation, OpenAI text-embedding-3-small for embeddings) are named only in the architecture doc; the RAG factory PDF names no models.",
  "UI/product direction (Stoic / How We Feel / Headspace components, Brilliant course presentation, 'low animation yet interactive') and the DeepTutor reference exist only in chat-barry.",
  "The LangExtract pilot on a single Tirthaji chapter (RAG PDF only) — outcome unknown.",
  "Preview-dashboard 'errata only' filter bug (q._errata vs q.errata) — dashboards report only; trivial but confirms the previews were shipped unfinished."
 ],
 "gaps": [
  "RFP v7.2 (2026-07-01, 365 specs) — the requirements authority every doc defers to — is in NO recovered source. Likely locations: client email thread, WhatsApp media (group or Tanish/Souvik chats), or the Google Drive folder. Without it, all spec-ID references (SUB-01, COG-07, GATE-08...) are unverifiable.",
  "Source code: neither the vedic-speed-gym monorepo (141 files, E:\\ Windows) nor Exam-Arena-Frontend (D:\\ Windows) exists in any known location; only 70 leaked TS files were recovered from the APK. Check the Drive zip, aniketthakur022's GitHub, and any CI remnants (GitHub Actions CI existed, implying a remote once existed) before declaring a from-scratch rebuild.",
  "The Google Drive folder is auth-gated and unenumerated — it supposedly holds the extraction/enrichment scripts, tracking dashboard, pipeline PDF, page_view.html/explore.html, and possibly the book PDFs. First concrete action: have the user open/download it.",
  "Source PDFs of the 25 books and the 64 intermediate per-book JSONL files — only names survive in source_file fields. Required for every vision re-pass and for Hall & Knight re-keying. Location unknown (Drive? Barry's machine?).",
  "Live cloud database state: unknown whether any cloud instances survive the laptop (pipeline-untitled.pdf implies Neo4j AuraDB Free; migrate.py explicitly supports cloud tiers with no psql). If an AuraDB graph or cloud Postgres still holds the 3,585-node graph and 4,943-chunk pgvector store, the rebuild shrinks dramatically. Nobody has checked.",
  "Deal terms and client status: quote amounts, scope, milestones (3 quote PDFs in WhatsApp media, unread), whether Souvik or Tanish is the client, whether the 2026-07-20 advance was for VMSG, and whether the client has been told about the data loss. Found in: WhatsApp group media + the quotation PDFs.",
  "Answers to the 8 open client decisions (payment region Stripe-vs-Razorpay, sampling carve-out, dormant models, bot/COPPA rules, Stage 1.5 infra, Kokoro TTS, content ownership, Reference Library option A/B/C) — no source records any resolution. Found in: client meeting notes/emails if they exist; otherwise must be re-asked.",
  "The OCR/digitizer tool for ingestion Station 1 is never named in ANY source — a tool must be chosen for the rebuild (DeepTutor's parser menu — MinerU, Docling, PyMuPDF4LLM — is the practical shortlist).",
  "The corpus→package transformation is undocumented: nothing explains how MASTER_corpus's 19,619 questions became the topic_browser package's 807 problems / 467 skills / 2,457 prereq edges, nor the taxonomy normalization from inconsistent corpus topic labels to the skill graph. The scripts in the Drive zip are the only plausible record.",
  "The Book Page View / page_view.html OCR-verification dashboard and the generator script that produces the preview HTMLs are not in local files — in the Drive zip per chat-barry, or must be rebuilt.",
  "Brand and deployment reality: is 'Exam Arena' (examarena.com) the go-forward brand or an abandoned direction? Is the domain registered? Was any backend ever deployed for the APK's /api/v1 contract? No source answers.",
  "Contents of the 3 unnamed PDFs, 4 screenshots, and the UUID-named Untitled.pdf from the 2026-09-01 delivery — unidentified; possibly in WhatsApp media alongside everything else.",
  "The Jul-5 → Sep-1 build history is a black hole: the progress report ends at Sprint 2 (2026-07-05), yet by Sep 1 an APK, demo videos, a 25-book corpus, and the RAG factory design existed. What else was built (Sprint 1 auth? Celery task stubs filled? closure Strategy A? Sprint 3?) is recorded nowhere.",
  "The two demo MP4s from the delivery have not been reviewed — they may show features (duel flow, backend-connected screens) that contradict the APK's mock-only state.",
  "Whether the ₹22k Claude subscription was purchased, and where the rebuild will live: the current Mac cwd (/Users/harshahirrao/Speed gym) is NOT a git repo and contains only docs/reference + data/corpus + apk — no code, no remote, no backups configured. Day-1 ops (git init + remote + backup policy + API-key rotation) has no owner yet."
 ],
 "workstream_split": {
  "backend": "MAIN BACKEND chat owns the app rebuild and all runtime services. Day 1: git init + remote + backup policy on the Mac, rotate the leaked Anthropic key, attempt code recovery (Drive zip, aniketthakur022 GitHub), and check whether any cloud DBs (AuraDB Free / cloud Postgres) survived before rebuilding schemas. Then re-author the monorepo per VMSG_TECHNICAL_ARCHITECTURE.md + VMSG_PROGRESS_REPORT.pdf: docker-compose dev stack, Postgres 15+pgvector migrations 00–70 (re-applying the 7 documented schema fixes: tier enum free/pro/bundle_2/bundle_3, UUID user_id, raw_events/chunks/user_technique_states tables, etc.), Neo4j constraints, Redis, PgBouncer; FastAPI with health/content/practice routers first; client-side TS packages (BKT with the fixed priors, 60/20/20 allocator, scheduler, calibration — the progress report proves this design worked, 46/46 tests); Next.js PWA seeded from the 70 recovered Exam-Arena sources in the scratchpad; Node Socket.IO stub; feature-flag service; /api/sync + HMAC offline entitlement. It owns two decisions that block everyone: (1) the API contract — architecture's JWT/QR 22-endpoint surface vs the APK's frozen /api/v1 Firebase-idToken contract (the APK is the only surviving frontend, so deviating means rebuilding the frontend too); (2) infra — self-hosted droplet Neo4j vs AuraDB Free (contradiction between architecture and pipeline PDFs). Sprint 1 payments (Stripe/Razorpay) stays parked until the client decisions are re-confirmed. It consumes, never produces, content: reads the graph, pgvector store, Redis tray, and the prerequisite_closure table.",
  "data_extraction": "DATA EXTRACTION chat owns MASTER_corpus.jsonl (/Users/harshahirrao/Speed gym/data/corpus/) as the canonical surviving asset — 4,805 records / 19,619 questions / 25 books, fully profiled and healthy. First: pull the Drive zip once the user authenticates (scripts, 64 per-book JSONLs, page_view.html/explore.html, ideally the source book PDFs) — do NOT rebuild tooling that may be sitting in that zip. Then: finish the confessedly incomplete verification dashboards (and fix the _errata filter bug), regenerate the stale preview HTMLs from the current 4,805-record corpus; run the vision re-pass backlog (4,133 needs_vision questions, 499 needs_chart_vision records, 146 untranscribed figures, 299 options_are_images, 459 unclassified formats — all with pdf_pages provenance for targeted re-reads); answer-key recovery in priority order (Sinha's 706 unkeyed — the priority book; Hall & Knight's 1,874 need a better scan or manual keys; Strang/Knuth/Mosteller via solution_note→answer derivation); re-read the 4 truncated GRE essays; normalize stimulus polymorphism, extra.data_points free-form dicts, and topic/tag casing into ONE canonical taxonomy; dedup the 168 duplicate texts. Deliverable to RAG: a flattened question-level export (set_id + question.number composite key) with canonical taxonomy labels, filtered to playable items (answer_key present, no needs_vision, usable question_format). gre_essays_corrected.jsonl is already subsumed — never re-ingest it.",
  "rag": "RAG chat owns the content factory per VMSG_RAG_Content_Factory.pdf under the 2026-07-17 scope lock (batch-only: nightly 00:00 UTC + hourly replenishment; ZERO runtime consumers; chatbot excluded — this supersedes the architecture doc's chatbot-retrieval design and must be re-confirmed with the owner once). Tasks: rebuild the 6-station ingestion lane, choosing the never-named Station-1 digitizer (evaluate DeepTutor's Apache-2.0 parser menu: MinerU first for scanned math PDFs, Docling second); settle trive-v2 vs LangExtract and rerun the Tirthaji pilot; regenerate the all-zero ontology-registry embeddings; clean the 4,943 chunks' messy type labels; jointly with backend, canonicalize :Skill (not :Technique) and resolve the PREREQUISITE_OF/REQUIRES edge-direction contradiction against whatever live graph survives; implement the 7-stage auditor + QUARANTINED→SANDBOX→TRUSTED→LIVE ladder with pattern freeze, T1 (1,505 templates) + T2 seeded generation with SymPy verification, the hourly T1–T5 escalation ladder, Redis 24h tray, token budgets and run reports; implement closure Strategy A (apoc.path.spanningTree → prerequisite_closure in Postgres) so the app never traverses Neo4j at runtime; fill the two Celery task stubs the main repo schedules. Critical output contract: emit content conforming to the APK's SolveAlongTemplate zod schema (concept{technique_name,category,sub_category}, difficulty 1–5, visual_scaffold from the 11 types, generationMethod template|llm|converted, sourceDocumentId) so the only surviving frontend can consume it, honoring its trusted(24h)/sandbox(1h) cache tiers. Also owns the corpus→graph mapping (data_extraction's flattened export → Skills/Problems/Traps), the 25 missing Reference Library pages (option A: auto-draft + human review), and rebuilding the 15-mock/3-blueprint exam content if it cannot be recovered."
 }
}