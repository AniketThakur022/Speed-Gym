"""Mock exams (block 6): authored blueprints, a pool-independent scoring
engine, and pluggable question sources. Nothing here depends on WHICH pool a
question came from — the owner's serving-path decision binds pools later."""
